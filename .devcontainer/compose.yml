# Reference: https://docs.docker.com/reference/compose-file/
name: cvrptw

services:
  devcontainer:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    command: bash -c "sleep infinity"
    container_name: devcontainer
    volumes:
      - ..:/workspaces/cvrptw
    working_dir: /workspaces/cvrptw
